{% extends 'base.html.twig' %}

{% block body %}


     <div class="container">
  <div class="row justify-content-center">
    <div class="col-xs-4 login m-3">
    <h1 class="p-3 headingLogin text-center m-3">Vytvoreni nove verze clanklu</h1>
    {{ form_start(form) }}
    <div class="form-group">
      {{ form_label(form.file, 'Soubor', {'label_attr': {'class': 'col-form-label col-sm-2 white text-center'}}) }}<br />
      {{ form_widget(form.file, {'attr': {'class': 'bg-red-soft text-center mx-auto'}}) }}
      </div>

      <div class="form-group">
      <button type="submit" class="btn btn-primary btn-light p-3 m-3 w-25">Podat</button>
      </div>
    {{ form_end(form) }}

    </div>
  </div>
</div>

{% endblock %}

